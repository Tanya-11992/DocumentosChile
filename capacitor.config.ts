import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MyDocumentosChile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
